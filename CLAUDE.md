# CLAUDE.md — AIgym プロジェクト固有ルール

## デザインファイル

- Figmaファイル: `2606_AI-GYM24--コピー-`
- ファイルキー: `BmPfc9DUL2nD5wrJgRdYwq`

---

## ブレイクポイント

| 名前 | 条件 |
|------|------|
| PC   | `min-width: 961px` |
| Tab  | `521px ～ 960px` |
| SP   | `max-width: 520px` |

---

## SPデザインのスケール換算ルール

- FigmaのSPデザインは **幅750px（@2x）** で作成されている
- 実際のブラウザ表示は **幅375px（@1x）** が基準
- **Figma数値 ÷ 2 = 実装値（px）**

### フォントサイズ変換例

| Figma値 | 実装値（rem） |
|---------|--------------|
| 60px    | 3rem         |
| 92px    | 4.6rem       |
| 40px    | 2rem         |
| 30px    | 1.5rem       |
| 88px    | 4.4rem       |
| 64px    | 3.2rem       |

### スペーシング変換例

| Figma値 | 実装値（px） |
|---------|-------------|
| 100px   | 50px        |
| 80px    | 40px        |
| 70px    | 35px        |
| 50px    | 25px        |
| 60px    | 30px        |

---

## レスポンシブ方針

- 変換後の実装値（px）を基準として `clamp` や `vw` でレスポンシブ対応する
- SP値が Figma 750px 基準で **Xpx** の場合、`vw` 係数は `X / 750 * 100`
  - 例: 60px → 実装3rem → `4vw`（60/750*100）で375pxのとき3rem相当
- clampの目安: `clamp(実装値/2, Xvw, 実装値)`
- Tabはclampで自然につなぐ。PCとSPのみデザインがある場合、Tabはその中間を埋める

---

## PCデザインのスケール

- FigmaのPCデザインは **幅1920px（@1x）** で作成されている
- Figma数値 = 実装値（そのまま使用）

---

## font-size 基底

- `destyle.css` で `html { font-size: 10px; }` 設定済み
- `1rem = 10px`
- フォントサイズはremで記述

---

## その他

- 上記以外は親ディレクトリの `CLAUDE.md`（共通ルール）に準拠する
